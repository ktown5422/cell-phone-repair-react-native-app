

const auth = (state, {type, payload}) => {
    switch (type) {
        case 'LoginScreen':
            return state;
        
        default:
            return state;
    }
};

export default auth;